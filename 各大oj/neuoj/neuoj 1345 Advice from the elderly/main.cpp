#include <iostream>
#include <bits/stdc++.h>
#include <algorithm>
using namespace std;

#define ll long long
#define maxn 1e5+5

struct node
{
    int v,c,t;
    node(int vv=0,int cc=0,int tt=0)
    {
        v=vv;
        c=cc;
        t=tt;
    }
    void input()
    {
        scanf("%d%d%d",&v,&c,&t);
    }
};

int main()
{
    struct node tem[maxn];
    int T;
    int num,money;
    scanf("%d",&T);
    while(T--)
    {
        scanf("%d%d",&num,&money);
        for(int i=0;i<num;i++)
    }
}
