#include <stdio.h>
#include <stdlib.h>

int main()
{
    int T,n,q,i,j,t;
    int a[1010],b[1010];
    while(scanf("%d",&T)!=EOF)
    {
        while(T--)
        {
            t=1;
            scanf("%d%d",&n,&m);
            for(i=0; i<n; i++) scanf("%d",&a[i]);
            for(i=0; i<m; i++) scanf("%d",&b[i]);
            if(n<m) printf("Never WOW.\n");
            else
            {
                for(i=0;i<n;i++)
                {
                    t=t+a
                }
            }

        }
    }
}
